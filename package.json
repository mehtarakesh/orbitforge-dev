{
  "name": "orbitforge-dev",
  "version": "0.1.0",
  "private": true,
  "description": "Public OrbitForge repo for desktop, CLI, and VS Code surfaces aligned to orbitforge.dev.",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build:extension": "cd apps/orbitforge-vscode && npm run build",
    "build:desktop": "cd apps/orbitforge-desktop && npm run build",
    "build:cli": "cd apps/orbitforge-cli && npm run build",
    "package:extension": "cd apps/orbitforge-vscode && npm run package",
    "package:desktop": "cd apps/orbitforge-desktop && npm run package",
    "package:desktop:mac": "cd apps/orbitforge-desktop && npm run package:mac",
    "package:desktop:win": "cd apps/orbitforge-desktop && npm run package:win",
    "package:desktop:linux": "cd apps/orbitforge-desktop && npm run package:linux",
    "package:cli": "cd apps/orbitforge-cli && npm pack"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
